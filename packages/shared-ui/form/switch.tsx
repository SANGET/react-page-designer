import { Switch as DSwitch } from '@deer-ui/core/switch-button';
import { basicComponentFac } from '../basic';

/**
 * 生成符合 UI 接入标准的 Switch
 */
const Switch = basicComponentFac(DSwitch);

export { Switch };
