import { WidgetMeta } from "..";
import { WidgetGroupingTypes } from "../grouping-register";

/**
 * 创建组件与属性项的绑定关系
 */
export const CreateWidgetPropItemRely = (meta: WidgetMeta<WidgetGroupingTypes>) => {
  return meta;
};